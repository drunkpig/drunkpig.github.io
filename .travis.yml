language: python
dist: bionic
sudo: required

python:
    - "3.7"

branches:
  only:
    - source
    - master

install:
  - pip install -U git+https://github.com/drunkpig/mdwiki.git
  - sudo snap install tree

script:
  - mdpub

